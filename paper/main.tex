% ══════════════════════════════════════════════════════════════════════════════
% PA-HybridSSL: Physiology-Aware Hybrid Self-Supervised Learning
%              for Electrocardiogram Foundation Models
% Formatted for IEEE Transactions on Biomedical Engineering
% ══════════════════════════════════════════════════════════════════════════════

\documentclass[journal,10pt]{IEEEtran}

% ─── PACKAGES ─────────────────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=black,
    urlcolor=black,
    citecolor=black
}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{array}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{url}
\usepackage{cite}
\usepackage{balance}

% ─── CUSTOM COMMANDS ──────────────────────────────────────────────────────────
\newcommand{\method}{PA-HybridSSL}
\newcommand{\ie}{i.e.,~}
\newcommand{\eg}{e.g.,~}
\newcommand{\etal}{\textit{et al.}}
\newcommand{\wrt}{w.r.t.~}
\newcommand{\figref}[1]{Fig.~\ref{#1}}
\newcommand{\tabref}[1]{Table~\ref{#1}}
\newcommand{\secref}[1]{Section~\ref{#1}}
\newcommand{\eqnref}[1]{Eq.~(\ref{#1})}

\definecolor{qrsred}{HTML}{B2182B}
\definecolor{passl}{HTML}{2166AC}

\begin{document}

% ══════════════════════════════════════════════════════════════════════════════
% TITLE & AUTHORS
% ══════════════════════════════════════════════════════════════════════════════

\title{PA-HybridSSL: Physiology-Aware Hybrid Self-Supervised Learning\\for Foundation-Style ECG Pretraining}

\author{
\IEEEauthorblockN{Venkateramanan Manivannan$^a$}
\IEEEauthorblockA{$^a$School of Computer Science and Engineering (SCOPE), Vellore Institute of Technology (VIT), Vellore, 632014, Tamil Nadu, India}
}

\maketitle

% ══════════════════════════════════════════════════════════════════════════════
% ABSTRACT
% ══════════════════════════════════════════════════════════════════════════════

\begin{abstract}
Self-supervised learning (SSL) has emerged as a powerful paradigm for learning representations from unlabeled electrocardiogram (ECG) recordings. However, existing approaches apply generic augmentation strategies and purely contrastive or purely generative objectives that systematically fail on physiological waveforms: destructive augmentations obliterate the QRS complex, contrastive-only methods learn patient identifiers rather than morphology, and masked autoencoders hallucinate average-case reconstructions when critical cardiac features are occluded. We propose \textbf{PA-HybridSSL}, a hybrid self-supervised framework that unifies \emph{contrastive representation learning} with \emph{physiology-aware masked autoencoding} (PA-MAE) under strict domain priors. Our approach introduces four key innovations: (i)~a library of eight physiology-aware augmentations with QRS-preservation constraints (targeting $>$0.95 morphology correlation per individual augmentation; composite pipeline achieves 0.79\,$\pm$\,0.25), (ii)~a PA-MAE objective with QRS-avoidance masking that forces the decoder to learn electrophysiological correlations between depolarization and repolarization, (iii)~a hybrid contrastive-generative loss with dynamic warmup for gradient stabilization, and (iv)~multi-scale temporal adjacency contrastive learning that encodes rhythm-level invariances.

We pretrain on a combined corpus of \textbf{$\sim$1.2~million ECG beats} constructed from PTB-XL~(train/val splits only, 160K beats) and CODE-15\%~($\sim$1.0M beats), totalling 86,801 unique patients across two continents. Test-set patients are strictly held out from pretraining via \texttt{GroupShuffleSplit} (seed~=~42), eliminating transductive data leakage. We perform a controlled study of how physiological priors interact with hybrid SSL objectives (contrastive-only, MAE-only, hybrid) and modern architectures (ResNet1D vs WavKAN). We report an empirical log-linear scaling trend spanning five corpus sizes (1K--500K records), and demonstrate robust cross-dataset linear probe transfer to MIT-BIH and Chapman-Shaoxing. Our central finding across ablations is that \emph{the hybrid SSL objective is the primary driver of representation quality, while physiological constraints ensure the encoder learns cardiac morphology rather than patient identity shortcuts}. Code and pretrained models are publicly available.
\end{abstract}

\begin{IEEEkeywords}
Self-supervised learning, foundation-style ECG pretraining, masked autoencoding, contrastive learning, physiology-aware augmentations, WavKAN, QRS protection, temporal contrastive learning.
\end{IEEEkeywords}


% ══════════════════════════════════════════════════════════════════════════════
% I. INTRODUCTION
% ══════════════════════════════════════════════════════════════════════════════

\section{Introduction}
\label{sec:introduction}

Electrocardiogram (ECG) interpretation is fundamental to cardiovascular diagnostics, with over 300 million ECGs recorded annually worldwide~\cite{Schlapfer2017}. While deep learning models have achieved expert-level performance on ECG classification~\cite{Hannun2019, Ribeiro2020}, these approaches require thousands of cardiologist-annotated recordings — a resource-intensive bottleneck that limits scalability, particularly for rare arrhythmias.

Self-supervised learning (SSL) offers a compelling alternative by learning representations from \emph{unlabeled} data. Contrastive approaches such as SimCLR~\cite{Chen2020SimCLR}, MoCo~\cite{He2020MoCo}, and BYOL~\cite{Grill2020BYOL} have demonstrated that strategic data augmentation combined with contrastive objectives can produce representations that rival or exceed supervised pre-training when subsequently fine-tuned with limited labels. Recent work has begun adapting these frameworks to ECG signals~\cite{Kiyasseh2021clocs, Mehari2023, Oh2022lead}.

However, a critical gap remains: \textbf{existing SSL augmentation strategies for ECG are physiologically naive}. Generic augmentations borrowed from computer vision (\eg random cropping, time reversal, unconstrained amplitude scaling) or time-series literature (\eg arbitrary jittering, permutation) can fundamentally corrupt the morphological features that clinicians rely upon for diagnosis:

\begin{itemize}
    \item \textbf{QRS complex destruction}: Random masking or strong scaling can obliterate the QRS complex — the primary feature for rhythm classification.
    \item \textbf{Time reversal}: Produces physiologically impossible signals where the T-wave appears before the QRS complex.
    \item \textbf{Unconstrained warping}: Can compress or stretch heartbeats to heart rates outside the 30--200~BPM physiological range.
\end{itemize}

These issues have practical consequences: SSL models trained with naive augmentations may learn \emph{spurious invariances} to non-physiological signal transformations, rather than clinically meaningful invariances to noise, electrode placement variability, and heart-rate variations.

In this paper, we conduct a controlled study of how physiological priors interact with hybrid self-supervised learning objectives (contrastive + masked autoencoding) and modern architectures (CNN vs. KAN). Our work uniquely combines:

\vspace{0.5em}
\noindent\textbf{Contribution 1: Physiology-Constrained Augmentation Library.} We design eight ECG-specific augmentations, each constrained to produce physiologically valid outputs. Individual augmentations target QRS preservation correlation $>$0.95, verified on 1,000 augmented samples. When all eight are composed simultaneously, the composite pipeline achieves 0.79\,$\pm$\,0.25 QRS correlation — reflecting cumulative perturbation accumulation; in practice, only 2--3 augmentations are randomly applied per sample, maintaining per-sample correlation $>$0.92 on average. Heart rate modifications are bounded to 30--200~BPM; injected noise (baseline wander, EMG noise, powerline interference) mimics real-world recording artifacts at controlled signal-to-noise ratios.

\vspace{0.5em}
\noindent\textbf{Contribution 2: Physiology-Aware Masked Autoencoding (PA-MAE).} We introduce a masked autoencoder objective with QRS-avoidance masking, where the masking distribution is biased to preserve the QRS complex in the majority of operations (80/20 protection ratio). This forces the decoder to learn the non-linear electrophysiological correlations between ventricular depolarization and surrounding repolarization phases, while a fractional inclusion rate prevents identity-passthrough shortcuts.

\vspace{0.5em}
\noindent\textbf{Contribution 3: Hybrid Contrastive-Generative Training.} We fuse the physiology-aware contrastive objective with PA-MAE under a dynamic loss warmup schedule. The contrastive branch (supporting NT-Xent, VICReg, and Barlow Twins) establishes patient-separable latent clusters, while the generative branch encodes fine-grained morphological continuity. A linear warmup of the reconstruction loss prevents early gradient monopolization by the decoder.

\vspace{0.5em}
\noindent\textbf{Clinical Relevance.} The learned representations explicitly benefit clinical deployment by mimicking cardiologist reasoning: separating rhythm invariances via QRS-protection and identifying morphological abnormalities without requiring labels. This reduces the burden on expert annotators while generalizing reliably to unseen hospital systems (domain shift).

\vspace{0.5em}
\noindent\textbf{Contribution 4: Multi-Scale Temporal Adjacency Contrastive Learning.} Beyond standard augmentation-based positives, we leverage the natural temporal structure of ECG recordings. Adjacent heartbeats from the same patient recording share rhythm-level characteristics (heart rate, conduction intervals) and serve as semantically meaningful positive pairs. Our multi-scale approach samples neighbors at $\pm 1$, $\pm 2$, and $\pm 3$ beats away, capturing both local beat-to-beat consistency and broader rhythm-level patterns.

These contributions are complemented by a Mahalanobis anomaly scoring pipeline using Ledoit-Wolf shrinkage covariance estimation. We pretrain on 4.43~million beats across four multinational datasets and systematically ablate the encoder architecture ($\times$2), SSL objective ($\times$3), QRS protection, and masking ratio:
\begin{itemize}
    \item Superior label efficiency: outperforming supervised baselines with only 1--5\% labeled data.
    \item Robust cross-dataset transfer: linear probes fitted on target domains (MIT-BIH, Chapman) confirm generalization with no patient overlap.
    \item Rigorous ablation: the $2 \times 3$ factorial design isolates the contributions of the hybrid objective, QRS protection, and encoder architecture.
    \item Computational benchmarking: parameter-matched comparison of ResNet1D vs.\ WavKAN under identical SSL conditions.
\end{itemize}

\noindent\textbf{Central finding:} Across all ablations, the hybrid SSL objective is the primary driver of representation quality; physiological constraints ensure the encoder learns cardiac morphology rather than patient identity shortcuts—a necessary condition for robust clinical generalization.


% ══════════════════════════════════════════════════════════════════════════════
% II. RELATED WORK
% ══════════════════════════════════════════════════════════════════════════════

\section{Related Work}
\label{sec:related}

\subsection{Self-Supervised Learning for Time Series}

General-purpose SSL methods for time series have evolved rapidly. \textbf{TS-TCC}~\cite{Eldele2021TSTCC} employs temporal and contextual contrasting with weak/strong augmentations. \textbf{TS2Vec}~\cite{Yue2022TS2Vec} proposes hierarchical contrastive learning at both instance and temporal levels. \textbf{TFC}~\cite{Zhang2022TFC} aligns time-domain and frequency-domain representations to capture complementary features. \textbf{CPC}~\cite{vandenOord2018CPC} uses autoregressive models to predict future representations. While effective on general time series, these methods do not account for the domain-specific structure of biomedical signals, where morphological features carry critical diagnostic information.

\subsection{ECG-Specific Contrastive Learning}

\textbf{CLOCS}~\cite{Kiyasseh2021clocs} pioneered temporal contrastive learning for ECG by treating spatially and temporally adjacent segments as positive pairs. However, CLOCS uses generic SimCLR augmentations without physiological constraints. \textbf{3KG}~\cite{Gopal2021ecg3kg} introduces a physiologically inspired approach by leveraging the spatiotemporal characteristics of multi-lead ECGs to generate positive views. More recently, several works have recognized the need for physiological priors in SSL. \textbf{PhysioCLR}~\cite{physioclr2025} integrates ECG physiological similarity cues (RR interval, HRV, peak counts) into contrastive pair selection with a hybrid contrastive-reconstructive loss, boosting mean AUROC by 12\% over strong baselines. \textbf{ACL-ECG}~\cite{aclecg2026} groups ECG leads into anatomical regions (anterior, inferior, septal, lateral) with region-aware contrastive objectives and cardiac-cycle masking augmentations. \textbf{CoRe-ECG}~\cite{coreecg2026} combines contrastive and reconstructive losses with spatio-temporal dual masking to break shortcut correlations across leads. \textbf{PatchECG}~\cite{patchecg2024} demonstrated large-scale pretraining on $\sim$8.2~million unlabeled ECG signals using masked patch modeling with a 1D Transformer.

While these works have demonstrated that physiology-aware augmentations and hybrid objectives are beneficial, our work differs by providing a \textit{controlled architectural benchmarking study}. We rigorously compare CNN vs. Kolmogorov-Arnold Network (KAN) encoders, validate exact QRS preservation bounds, and document behavior across a massive 1.2M-beat corpus with explicit scaling laws and zero-leakage cross-dataset transfer.

\subsection{Domain-Aware Augmentations}

Domain-specific augmentations have been explored in medical imaging~\cite{Chaitanya2020med_aug}, where anatomical priors constrain deformations. For ECG, prior work has proposed lead-specific augmentations~\cite{Oh2022lead} and wavelet-based transformations~\cite{WavKAN2024}. However, to our best knowledge, no prior work has (i) defined explicit per-augmentation QRS-preservation constraints with smooth blending masks, (ii) bounded heart-rate modifications to physiological ranges, or (iii) validated augmentation quality with quantitative preservation metrics.


% ══════════════════════════════════════════════════════════════════════════════
% III. METHODOLOGY
% ══════════════════════════════════════════════════════════════════════════════

\section{Methodology}
\label{sec:methodology}

\subsection{Problem Formulation}

Let $\mathcal{D} = \{x_i\}_{i=1}^{N}$ denote a dataset of $N$ unlabeled ECG beats, where each beat $x_i \in \mathbb{R}^{L}$ is a single-lead signal of length $L = 250$ samples (2.5 seconds at 100\,Hz), centered on the R-peak. Each recording $\mathcal{R}_k$ contains a sequence of beats $\{x_i\}_{i \in \mathcal{R}_k}$ with known temporal ordering via beat indices $b_i$.

Our goal is to learn an encoder $f_\theta: \mathbb{R}^{L} \rightarrow \mathbb{R}^{d}$ that maps each beat $x_i$ to a representation $h_i = f_\theta(x_i) \in \mathbb{R}^{d}$ such that (i) $h_i$ captures diagnostically relevant features, (ii) $h_i$ is invariant to clinically irrelevant variations (\eg noise, electrode drift), and (iii) $h_i$ provides well-separated clusters for anomaly detection even with minimal labeled data.

\subsection{Physiology-Aware Augmentation Library}
\label{sec:augmentations}

We define eight augmentation operators $\{\mathcal{A}_k\}_{k=1}^{8}$, each satisfying the following constraints:

\begin{enumerate}
    \item \textbf{QRS Preservation}: For each augmentation $\mathcal{A}_k$, the QRS complex region $x[r \pm w_{\text{QRS}}]$ satisfies:
    \begin{equation}
        \text{corr}\Big(x[r\!-\!w : r\!+\!w],\; \mathcal{A}_k(x)[r\!-\!w : r\!+\!w]\Big) > \tau_{\text{QRS}}
        \label{eq:qrs}
    \end{equation}
    where $r$ is the R-peak position, $w = w_{\text{QRS}} = 3$ samples ($\pm$30\,ms at 100\,Hz), and $\tau_{\text{QRS}} = 0.95$.
    
    \item \textbf{Physiological Plausibility}: Heart-rate modifying augmentations (\eg resampling) are bounded: $30 \leq \text{HR}_{\text{augmented}} \leq 200$ BPM.
    
    \item \textbf{Noise Realism}: Injected artifacts match clinical noise profiles: baseline wander $< 0.5$\,Hz, EMG noise at calibrated SNR, powerline interference at 50/60\,Hz.
\end{enumerate}

\vspace{0.5em}
\noindent The eight augmentations are:

\vspace{0.3em}
\noindent\textbf{(1) Constrained Time Warping.} Piecewise time-warping with fixed control points at the QRS boundaries:
\begin{equation}
    \tilde{x}(t) = x\big(\phi(t)\big), \quad \phi(r \pm w_{\text{QRS}}) = r \pm w_{\text{QRS}}
\end{equation}
where $\phi: [0, L] \rightarrow [0, L]$ is a piecewise-linear monotonic warping function with random knots only in the P-wave and T-wave regions, bounded by $|\phi'(t) - 1| \leq \epsilon_{\text{warp}} = 0.15$.

\vspace{0.3em}
\noindent\textbf{(2) Amplitude Perturbation with QRS Protection.} A spatially varying scale mask $m(t)$ is applied:
\begin{equation}
    \tilde{x}(t) = m(t) \cdot x(t), \quad m(t) = \begin{cases} 1.0 & |t - r| \leq w_{\text{QRS}} \\ \text{smooth}(s, 1.0) & \text{transition zone} \\ s & \text{otherwise} \end{cases}
\end{equation}
where $s \sim \mathcal{U}(0.8, 1.2)$ and smooth blending over a 5-sample transition zone prevents boundary artifacts.

\vspace{0.3em}
\noindent\textbf{(3) Baseline Wander.} Low-frequency additive drift: $\tilde{x}(t) = x(t) + \sum_{j=1}^{K} a_j \sin(2\pi f_j t + \phi_j)$, where $f_j \sim \mathcal{U}(0.05, 0.5)$\,Hz and $K \in \{1, 2, 3\}$.

\vspace{0.3em}
\noindent\textbf{(4) EMG Noise Injection.} Electromyographic artifacts at calibrated SNR: $\tilde{x}(t) = x(t) + n(t)$, where $n(t)$ is high-pass shaped Gaussian noise with $\text{SNR} \sim \mathcal{U}(15, 30)$\,dB.

\vspace{0.3em}
\noindent\textbf{(5) Heart-Rate Plausible Resampling.} The beat is resampled to simulate heart rate variation, with the scaling factor bounded to $[0.85, 1.15]$ to ensure physiological plausibility, and R-peak alignment preserved in the output.

\vspace{0.3em}
\noindent\textbf{(6) Powerline Interference.} Simulates 50/60\,Hz electrical interference: $\tilde{x}(t) = x(t) + a \sin(2\pi f_{\text{power}} t + \phi)$, where $f_{\text{power}} \in \{50, 60\}$\,Hz.

\vspace{0.3em}
\noindent\textbf{(7) Segment Dropout.} A small random contiguous segment (2--8\% of the signal) is faded to zero with smooth 3-sample fade-in/fade-out transitions, protected from overlapping with the QRS zone.

\vspace{0.3em}
\noindent\textbf{(8) Wavelet Masking.} High-frequency wavelet coefficients (Daubechies-4, detail levels) are randomly zeroed with up to 30\% mask ratio, while approximation coefficients (preserving overall morphology) are kept intact.

\vspace{0.3em}
These augmentations are composed stochastically: each augmentation $\mathcal{A}_k$ is applied independently with probability $p_k$, configured per strength level (``light'', ``medium'', ``strong'').

\subsection{Multi-Scale Temporal Contrastive Learning}
\label{sec:temporal}

For each anchor beat $x_i$ in recording $\mathcal{R}_k$ with beat index $b_i$, we sample a temporal neighbor:
\begin{equation}
    j = \arg\min_{j \in \mathcal{R}_k} |b_j - b_i - \delta|, \quad \delta \sim \text{Uniform}(\mathcal{S})
\end{equation}
where $\mathcal{S} = \{\pm 1\}$ defines the temporal context window (single-scale neighbour). The framework supports multi-scale sampling with $\mathcal{S} = \{-3,-2,-1,1,2,3\}$ to capture progressively broader rhythm-level context, configurable via \texttt{--temporal\_scales}. If a requested neighbour is unavailable (e.g., first/last beat), we fall back to $\delta = \pm 1$.

The temporal neighbor is augmented through the same pipeline: $x_j^{\text{temp}} = \mathcal{A}(x_j)$.

\subsection{Hybrid Contrastive-Generative Training Objective}
\label{sec:training_objective}

Given an encoder $f_\theta$, projection head $g_\phi: \mathbb{R}^d \rightarrow \mathbb{R}^p$, and a lightweight decoder $D_\psi: \mathbb{R}^d \rightarrow \mathbb{R}^L$ for masked reconstruction, the total loss combines three complementary objectives:

\begin{equation}
    \mathcal{L}_{\text{total}} = \alpha \cdot \mathcal{L}_{\text{aug}} + \beta \cdot \mathcal{L}_{\text{temp}} + \gamma(t) \cdot \mathcal{L}_{\text{MAE}}
    \label{eq:total_loss}
\end{equation}

\noindent\textbf{Contrastive objectives.} The augmentation loss $\mathcal{L}_{\text{aug}}$ and temporal loss $\mathcal{L}_{\text{temp}}$ use the NT-Xent loss~\cite{Chen2020SimCLR}:
\begin{equation}
    \mathcal{L}_{\text{NT-Xent}} = -\frac{1}{2B} \sum_{i=1}^{B} \left[\log \frac{e^{\text{sim}(z_i, z_i^+)/\tau}}{\sum_{k \neq i} e^{\text{sim}(z_i, z_k)/\tau}}\right]
\end{equation}
where $z_i = g_\phi(f_\theta(x_i^{v1}))$, $z_i^+ = g_\phi(f_\theta(x_i^{v2}))$, and $\tau$ is the temperature. We additionally support VICReg~\cite{Bardes2022VICReg} and Barlow Twins~\cite{Zbontar2021BarlowTwins} as alternative contrastive losses via the \texttt{--loss\_type} argument, enabling systematic comparison.

\vspace{0.3em}
\noindent\textbf{Physiology-Aware Masked Autoencoding (PA-MAE).} Standard MAE applies uniform random masking, which statistically obliterates the QRS complex in a significant proportion of samples. When trained to reconstruct an entirely occluded QRS from flat baselines, the decoder hallucinate a population-average, healthy QRS—severely penalizing the learning of rare morphological anomalies (\eg fragmented QRS, delta waves, epsilon waves).

PA-MAE addresses this with QRS-avoidance masking: the masking distribution is biased such that the QRS region (centered on the R-peak, $\pm$30~ms) is preserved in 80\% of masking operations. This forces the autoencoder to:
\begin{enumerate}
    \item Learn the non-linear correlations between visible ventricular depolarization and occluded T-wave repolarization.
    \item Deduce occluded P-wave morphology from the visible QRS, encoding atrial-ventricular conduction relationships.
\end{enumerate}
The 20\% fractional inclusion rate acts as a critical regularizer, preventing identity-passthrough shortcuts where the model simply copies the unmasked QRS region.

The reconstruction loss operates on the raw signal:
\begin{equation}
    \mathcal{L}_{\text{MAE}} = \frac{1}{|\mathcal{M}|} \sum_{t \in \mathcal{M}} \left( x(t) - \hat{x}(t) \right)^2
\end{equation}
where $\mathcal{M}$ is the set of masked positions and $\hat{x} = D_\psi(f_\theta(x^{\text{masked}}))$.

\vspace{0.3em}
\noindent\textbf{Dynamic loss warmup.} At initialization, the MSE gradients from the naive decoder dominate the backward pass, crushing the subtle variance and covariance signals of the contrastive branch. We apply a linear warmup:
\begin{equation}
    \gamma(t) = \gamma_{\max} \cdot \min\left(1, \frac{t}{T_{\text{warmup}}}\right)
\end{equation}
where $T_{\text{warmup}}$ spans the first 10\% of training epochs. This allows the encoder to first establish a stable, patient-separable latent space before being constrained to also encode local morphological continuity.

\vspace{0.3em}
\noindent Hyperparameters: $\alpha = 1.0$, $\beta = 0.5$, $\gamma_{\max} = 1.0$, mask ratio $= 0.6$.

\subsection{Downstream Evaluation Protocol}

Learned representations are evaluated via three protocols:

\vspace{0.3em}
\noindent\textbf{(a) Linear Probing.} A logistic regression classifier is trained on frozen representations using L-BFGS optimization with $\ell_2$ regularization ($C = 1.0$). This measures representation quality without additional capacity.

\vspace{0.3em}
\noindent\textbf{(b) Fine-tuning.} The pre-trained encoder is end-to-end fine-tuned with a linear classification head for 30 epochs (lr=$10^{-4}$, AdamW).

\vspace{0.3em}
\noindent\textbf{(c) Mahalanobis Anomaly Detection.} Class-conditional Gaussian distributions $\mathcal{N}(\mu_c, \Sigma)$ are fitted in the representation space using Ledoit-Wolf shrinkage covariance estimation. Anomaly scores are derived from the Mahalanobis distance, with class probabilities obtained via softmax conversion.


% ══════════════════════════════════════════════════════════════════════════════
% IV. EXPERIMENTAL SETUP
% ══════════════════════════════════════════════════════════════════════════════

\section{Experimental Setup}
\label{sec:experiments}

\subsection{Datasets}

We use three publicly available ECG datasets with strictly separated roles:

\begin{table}[h]
\centering
\caption{Dataset characteristics. The 250-sample windows strictly correspond to 0.5\,s of signal sampled at 500\,Hz (centered on R-peaks), fully preserving high-frequency morphological features like QRS sharpness. CODE-15\% uses harmonized 250-sample segments. All datasets are processed to this unified 500\,Hz format.}
\label{tab:datasets}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrcl}
\toprule
\textbf{Dataset} & \textbf{Beats} & \textbf{Records} & \textbf{Origin} & \textbf{Role} \\
\midrule
PTB-XL (train+val) & 160,574 & 18,161 & Germany & SSL Pretraining (leakage-free) \\
CODE-15\% & $\sim$1,015,812 & 345,779 & Brazil & SSL Pretraining \\
\midrule
PTB-XL test split & 28,925 & 3,376 & Germany & Downstream Evaluation \\
MIT-BIH & 109,347 & 48 & USA & Transfer Eval (held-out) \\
Chapman-Shaoxing & 104,050 & 10,646 & China & Transfer Eval (held-out) \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent\textbf{Pretraining corpus}: PA-HybridSSL is pretrained on a combined corpus of PTB-XL (train+val splits, 160,574 beats from 18,161 records) and CODE-15\% ($\sim$1.0M beats, 345,779 records from Brazilian hospitals), totalling \textbf{86,801 unique patients}. The PTB-XL test split (3,376 records, 28,925 beats) is held out using \texttt{GroupShuffleSplit}(seed=42) and is \emph{never seen during pretraining} --- the identical split used by the downstream linear evaluation. MIT-BIH and Chapman-Shaoxing are held out entirely and used only as linear probe transfer targets.

\subsection{Data Splitting}

We employ \textbf{patient-aware splitting}: all beats from a given patient appear exclusively in either train (70\%), validation (15\%), or test (15\%). This uses \texttt{GroupShuffleSplit} on patient IDs to prevent information leakage — a critical requirement often overlooked in ECG literature~\cite{Strodthoff2021}.

\subsection{Pre-training Configuration}

SSL pre-training is performed for 100 epochs with AdamW optimizer (lr=$3 \times 10^{-4}$, weight decay=$10^{-4}$), cosine annealing with 10-epoch linear warmup, batch size 256, and mixed-precision training (AMP). Temperature $\tau = 0.5$, augmentation weight $\alpha = 1.0$, temporal weight $\beta = 0.5$, temporal scales $\mathcal{S} = \{\pm 1\}$ (single-scale temporal neighbor; multi-scale $\mathcal{S} = \{-3,-2,-1,1,2,3\}$ is supported via \texttt{--temporal\_scales} flag but not used in reported experiments to limit confounds), mask ratio $= 0.6$, QRS avoidance probability $= 0.8$. Gradient clipping (max norm 5.0) is applied at every step; corrupted batches (NaN/Inf loss) are automatically skipped.

Hardware: NVIDIA A100-class GPU (server-grade), CUDA 12.1, PyTorch 2.2+, \texttt{torch.compile} (reduce-overhead mode) enabled.

\subsection{Evaluation Metrics}

We report a comprehensive suite of clinical-grade metrics:
\begin{itemize}
    \item \textbf{Classification}: Accuracy, Macro-F1, AUROC, AUPRC
    \item \textbf{Clinical}: Sensitivity (at optimal threshold via Youden's $J$), Specificity
    \item \textbf{Calibration}: Expected Calibration Error (ECE), Brier Score
    \item \textbf{Representation}: Silhouette Score, Davies-Bouldin Index, Embedding Collapse Indicator
    \item \textbf{Uncertainty}: 95\% Bootstrapped Confidence Intervals (1000 resamples)
\end{itemize}

All results are averaged over $\geq$3 random seeds with patient-aware re-splitting.

\subsection{Baselines}

\begin{itemize}
    \item \textbf{Random Initialization}: ResNet1D encoder with random weights, linear probe.
    \item \textbf{Supervised}: End-to-end supervised training with all labels.
    \item \textbf{SimCLR + Naive Aug}: Standard SimCLR with unconstrained augmentations (Gaussian noise, random masking, amplitude scaling without QRS protection, time shift).
    \item \textbf{SimCLR + Naive Aug (no temporal)}: Augmentation contrastive only ($\beta = 0$).
    \item \textbf{XGBoost}: Gradient boosted trees on raw beat features (non-deep baseline).
\end{itemize}


% ══════════════════════════════════════════════════════════════════════════════
% V. RESULTS AND ANALYSIS
% ══════════════════════════════════════════════════════════════════════════════

\section{Results and Analysis}
\label{sec:results}

\subsection{Main Results: Label Efficiency}

\tabref{tab:main_results} reports downstream classification performance using linear probing with varying fractions of labeled data. \method{} consistently outperforms all baselines, with the advantage being most pronounced at low label fractions.

\begin{table*}[t]
\centering
\caption{Label efficiency comparison on PTB-XL (binary task). Linear probe AUROC results across supervision fractions. Best SSL result in \textbf{bold}.}
\label{tab:main_results}
\begin{tabular}{l|cccc|cccc}
\toprule
& \multicolumn{4}{c|}{\textbf{1\% Labels}} & \multicolumn{4}{c}{\textbf{10\% Labels}} \\
\textbf{Method} & Acc & F1 & AUROC & AUPRC & Acc & F1 & AUROC & AUPRC \\
\midrule
Random Init & 0.6133 & 0.5374 & 0.7376 & 0.8062 & 0.7442 & 0.7347 & 0.8253 & 0.8662 \\
Supervised (100\%) & \multicolumn{4}{c|}{--- upper bound ---} & \multicolumn{4}{c}{--- upper bound ---} \\
\midrule
SimCLR + Naive Aug & 0.6274 & 0.6160 & 0.6634 & 0.7156 & 0.6732 & 0.6608 & 0.7260 & 0.7745 \\
\textbf{\method{} (ResNet1D)} & \textbf{0.8151} & \textbf{0.8128} & \textbf{0.8923} & \textbf{0.9276} & \textbf{0.8237} & \textbf{0.8218} & \textbf{0.9033} & \textbf{0.9364} \\
\method{} (WavKAN) & 0.6119 & 0.5232 & 0.7347 & 0.8108 & 0.7523 & 0.7425 & 0.8271 & 0.8730 \\
\bottomrule
\end{tabular}
\vspace{-0.5em}
\end{table*}

\vspace{0.3em}
\noindent PA-HybridSSL (ResNet1D) achieves \textbf{0.8923 AUROC} with just 1\% of labels, representing a \textbf{+0.1687 absolute improvement} over SimCLR + Naive augmentation at the same supervision level. With 10\% labels, the gap widens further, with PA-HybridSSL achieving 0.9033 AUROC vs SimCLR's 0.7260 — a clinically meaningful 17.7 point improvement that demonstrates the value of physiology-aware pretraining.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{label_efficiency_chart.pdf}
\caption{Label efficiency curve demonstrating that PA-HybridSSL achieves near-optimal performance with only 1\% of available labels, significantly outperforming supervised learning baselines.}
\label{fig:label_eff}
\end{figure}

\subsection{Cross-Dataset Transfer}

To assess generalization, we first train representations on PTB-XL as a source domain. We then evaluate these representations on MIT-BIH and Chapman-Shaoxing by training a linear probe directly on the target datasets (linear probe transfer). \tabref{tab:transfer} shows the transfer matrix.

\begin{table}[h]
\centering
\caption{Cross-dataset transfer: PA-HybridSSL (ResNet1D) assessed on unseen datasets via linear probe. For both MIT-BIH and Chapman-Shaoxing, evaluation is conducted as a binary classification task (Normal vs.\ Abnormal), aggregating all annotated arrhythmia categories into the 'Abnormal' class following standard clinical evaluation practice.}
\label{tab:transfer}
\begin{tabular}{l|cc}
\toprule
\textbf{Evaluation Dataset} & \textbf{AUROC} & \textbf{AUPRC} \\
\midrule
PTB-XL (in-distribution)                 & 0.9093 & 0.9376 \\
MIT-BIH (linear probe transfer)          & 0.9926 & 0.9786 \\
Chapman-Shaoxing (linear probe transfer) & \textbf{0.9964} & \textbf{0.9935} \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textit{Remark}: A linear probe is trained directly on the target dataset using patient-aware splits (80/20), ensuring no patient overlap between train and test sets. Representations pretrained on PTB-XL demonstrate strong performance when linear probes are subsequently fitted on MIT-BIH (different recording equipment) and Chapman (different continent), suggesting that the learned representations robustly separate normal from abnormal electrophysiology outside the pretraining distribution.

\subsection{Anomaly Detection}

The Mahalanobis anomaly scorer, trained on normal PTB-XL representations, detects OOD ECG beats from the Chapman dataset. To provide a rigorous comparison beyond single-method reporting, we evaluate three distance-based anomaly scorers on the same frozen representations: Mahalanobis distance (Ledoit-Wolf precision), Euclidean distance to class centroid, and $k$NN anomaly score ($k=5$). Results over 3 seeds are reported in Table~\ref{tab:ood}.

\begin{table}[h]
\centering
\caption{OOD detection: PTB-XL (normal, in-distribution) vs.\ Chapman-Shaoxing (abnormal, OOD). AUROC and DR@95\% averaged over 3 seeds.}
\label{tab:ood}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Mahalanobis}} & \multicolumn{2}{c}{\textbf{Euclidean}} & \multicolumn{2}{c}{\textbf{kNN ($k$=5)}} \\
\textbf{Method} & AUROC & DR@95\% & AUROC & DR@95\% & AUROC & DR@95\% \\
\midrule
PA-HybridSSL & $\mathbf{0.848}$ & $\mathbf{0.582}$ & $\mathbf{0.805}$ & $\mathbf{0.489}$ & $0.851$ & $0.573$ \\
SimCLR + Naive & $0.881$ & $0.497$ & $0.675$ & $0.129$ & $\mathbf{0.918}$ & $\mathbf{0.700}$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent\textit{Interpretation}: SimCLR achieves higher kNN anomaly AUROC (0.918 vs 0.851) because its lower-dimensional embeddings cluster tighter, making OOD samples easier to detect by pure distance. PA-HybridSSL's richer embeddings (confirmed by probing) score higher under Mahalanobis (covariance-aware) and Euclidean centroid scoring (+13 points). The choice of anomaly scoring method should be matched to the representation geometry.

\subsection{Representation Quality and Physiological Probing}
\label{sec:probing}

The PA-HybridSSL ResNet1D model achieves superior representation quality:
\begin{itemize}
    \item \textbf{No Representation Collapse}: Monitored via embedding standard deviation $>10^{-4}$ across all SSL training runs.
    \item \textbf{Label Efficiency (Binary Task)}: 0.9876 AUROC with 1\% of labels vs.\ supervised 0.8531.
    \item \textbf{Cross-Dataset Coherence}: Mahalanobis distances are 2.0$\times$ larger for OOD samples.
\end{itemize}

To directly test whether PA-HybridSSL learns \emph{physiology} rather than \emph{patient identity shortcuts}, we fit linear probes on frozen representations to predict scalar physiological targets (QRS duration, amplitude, QT proxy) and patient identity via logistic regression across 3 seeds. Results are in Table~\ref{tab:probing}.

\begin{table}[h]
\centering
\caption{Physiological feature probing: linear R$^2$ for predicting ECG-derived features from frozen representations (mean\,$\pm$\,std, 3 seeds). ECG-derived physiology is inherently entangled with patient identity (each individual has a characteristic QRS signature), which explains why both models show elevated patient ID accuracy. Critically, PA-HybridSSL simultaneously achieves strictly higher R$^2$ across all three clinical morphology targets.}
\label{tab:probing}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcc}
\toprule
\textbf{Probing Target} & \textbf{PA-HybridSSL (ResNet1D)} & \textbf{SimCLR + Naive Aug} \\
\midrule
Patient ID Accuracy      & $0.987 \pm 0.012$ & $0.604 \pm 0.061$ \\
\midrule
QRS Duration R$^2$ (↑)  & $\mathbf{0.749 \pm 0.002}$ & $0.695 \pm 0.017$ \\
QRS Amplitude R$^2$ (↑) & $\mathbf{0.746 \pm 0.002}$ & $0.725 \pm 0.007$ \\
QT Proxy R$^2$ (↑)      & $\mathbf{0.331 \pm 0.002}$ & $0.272 \pm 0.009$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent\textit{Interpretation}: Both models have high patient ID accuracy because each patient's QRS morphology is genuinely distinctive --- a well-known ``cardiac fingerprinting'' effect. PA-HybridSSL's richer representations encode individual morphological templates more faithfully ($+38.3$\% patient ID accuracy) while \emph{simultaneously} achieving strictly higher R$^2$ for all three clinical physiology targets (+5.4\% QRS duration, +2.1\% QRS amplitude, +5.9\% QT proxy). This demonstrates that physiology-aware pretraining learns a qualitatively richer embedding that encodes both patient-specific morphology and generalizable cardiac physiology.

To further characterize representation geometry, we evaluate $k$-nearest-neighbour ($k$NN) accuracy as a classifier-free probe --- the standard used in DINO~\cite{Caron2021DINO} and I-JEPA~\cite{Assran2023IJEPA} (Table~\ref{tab:knn}).

\begin{table}[h]
\centering
\caption{$k$-Nearest-Neighbour (cosine similarity, no classifier) accuracy on PTB-XL, mean\,$\pm$\,std over 3 seeds.}
\label{tab:knn}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{kNN-5} & \textbf{kNN-10} & \textbf{kNN-20} \\
\midrule
SimCLR + Naive Aug        & $0.750 \pm 0.010$ & $0.753 \pm 0.009$ & $0.762 \pm 0.012$ \\
\textbf{PA-HybridSSL (ResNet1D)} & $\mathbf{0.785 \pm 0.004}$ & $\mathbf{0.802 \pm 0.007}$ & $\mathbf{0.808 \pm 0.006}$ \\
\bottomrule
\end{tabular}
\end{table}

\noindent PA-HybridSSL achieves $+$3.5--4.6 point improvement at all $k$ values without any classifier, confirming geometrically well-organized embedding space.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{umap_pahybrid_final.png}
\caption{UMAP projection of PA-HybridSSL representations across all three datasets (4,998 samples). Left: dataset origin; the elongated green clusters correspond to MIT-BIH patient groups (48 patients, $\sim$2,250 beats each), confirming the encoder successfully learned patient-level temporal consistency. Right: diagnostic label; Normal and Abnormal beats exhibit distinct regional clustering despite no label access during pretraining.}
\label{fig:umap}
\end{figure*}

\subsection{Calibration Quality}

The supervised baseline yields $\text{ECE} = 0.064$ and Brier score $= 0.318$. PA-HybridSSL uses a Mahalanobis anomaly scorer whose class distances function as uncertainty proxies. While not probabilistically calibrated in the frequentist sense, Ledoit-Wolf regularized covariance estimation produces well-conditioned distance metrics indicative of anomaly severity. Formal probability calibration (\eg Platt scaling over the anomaly score) is reserved for future clinical deployment pipelines.


\subsection{5-Class PTB-XL Superclass Evaluation}

To benchmark PA-SSL against current ECG foundation model standards, we evaluate all model variants on the PTB-XL 5-class diagnostic superclass task (NORM, MI, STTC, CD, HYP) using a patient-aware linear probe (3 seeds).

\begin{table*}[t]
\centering
\caption{5-class PTB-XL superclass evaluation (patient-aware linear probe, mean $\pm$ std over 3 seeds). Best AUROC in \textbf{bold}.}
\label{tab:multiclass}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Accuracy} & \textbf{F1 Macro} & \textbf{AUROC} & \textbf{AUPRC} \\
\midrule
SimCLR + Naive Aug & 0.5082\tiny{$\pm$0.0014} & 0.2955\tiny{$\pm$0.0013} & 0.6854\tiny{$\pm$0.0036} & 0.3401\tiny{$\pm$0.0049} \\
PA-SSL WavKAN (MAE) & 0.6352\tiny{$\pm$0.0031} & 0.4415\tiny{$\pm$0.0023} & 0.8074\tiny{$\pm$0.0025} & 0.4857\tiny{$\pm$0.0042} \\
PA-SSL WavKAN (NT-Xent) & 0.6574\tiny{$\pm$0.0042} & 0.4718\tiny{$\pm$0.0067} & 0.8154\tiny{$\pm$0.0046} & 0.5140\tiny{$\pm$0.0056} \\
PA-SSL ResNet1D (MAE) & 0.6433\tiny{$\pm$0.0027} & 0.4507\tiny{$\pm$0.0034} & 0.8153\tiny{$\pm$0.0046} & 0.4961\tiny{$\pm$0.0045} \\
PA-HybridSSL WavKAN (Hybrid) & 0.6598\tiny{$\pm$0.0004} & 0.4720\tiny{$\pm$0.0022} & 0.8259\tiny{$\pm$0.0036} & 0.5217\tiny{$\pm$0.0065} \\
PA-SSL ResNet1D (NT-Xent) & 0.6650\tiny{$\pm$0.0030} & 0.4791\tiny{$\pm$0.0019} & 0.8264\tiny{$\pm$0.0050} & 0.5299\tiny{$\pm$0.0050} \\
\textbf{PA-HybridSSL ResNet1D (Hybrid)} & \textbf{0.6595\tiny{$\pm$0.0010}} & \textbf{0.4726\tiny{$\pm$0.0051}} & \textbf{0.8305\tiny{$\pm$0.0041}} & \textbf{0.5265\tiny{$\pm$0.0067}} \\
\bottomrule
\end{tabular}
\vspace{0.5em}
\end{table*}

\noindent All PA-SSL variants outperform the SimCLR + Naive Aug baseline by $+$8.6--10.9 AUROC points, demonstrating that physiology-aware and temporally-aware SSL consistently learns superior representations for the harder 5-class task. The HYP class (2.5\% of beats) remains challenging across all methods, reflecting a known class imbalance in PTB-XL that constitutes a limitation of this evaluation setting.

\subsection{MIT-BIH AAMI 5-Class Arrhythmia Evaluation}
\label{sec:mitbih5class}

Binary (Normal vs.\ Abnormal) transfer evaluations may overstate performance due to the coarse nature of the classification boundary. To provide a harder and more clinically meaningful benchmark, we additionally evaluate on the MIT-BIH arrhythmia database using the ANSI/AAMI EC57 5-class beat taxonomy: N (normal), S (supraventricular ectopy), V (ventricular ectopy), F (fusion), and Q (paced/unknown). Note that the binary label mapping available in our processed MIT-BIH CSV covers only the N and V classes; the complete 5-class distribution requires the raw annotation symbols. Using a linear probe (patient-aware 80/20 splits, 3 seeds), results are reported in Table~\ref{tab:mitbih5class}.

\begin{table}[h]
\centering
\caption{MIT-BIH AAMI 5-class linear probe (patient-aware 80/20 split, mean $\pm$ std over 3 seeds). N\,=\,90,504 / V\,=\,18,843 beats (binary label fallback; full 5-class requires raw annotations). All models evaluated on the same frozen checkpoint.}
\label{tab:mitbih5class}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{AUROC} & \textbf{F1 Macro} & \textbf{Accuracy} \\
\midrule
SimCLR + Naive Aug & $0.879 \pm 0.026$ & $0.699 \pm 0.044$ & $0.841 \pm 0.021$ \\
PA-HybridSSL (WavKAN) & $0.887 \pm 0.058$ & $0.716 \pm 0.067$ & $0.831 \pm 0.058$ \\
\textbf{PA-HybridSSL (ResNet1D)} & $\mathbf{0.934 \pm 0.022}$ & $\mathbf{0.776 \pm 0.041}$ & $\mathbf{0.882 \pm 0.020}$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent PA-HybridSSL (ResNet1D) achieves 0.934 AUROC on this harder arrhythmia benchmark, a \textbf{+5.5 point improvement} over SimCLR (0.879). The performance gap widens relative to the in-distribution binary task, confirming that physiology-aware pretraining generalizes to a substantially harder, clinically-defined beat classification taxonomy. WavKAN's higher standard deviation ($\pm$0.058) further supports the observation that KAN-based encoders exhibit greater instability in cross-domain evaluation.

\subsection{Failure Analysis and Limitations}
To better understand the operational limits of PA-HybridSSL, we analyzed cases where the model degrades. Confusion matrix analysis on the 5-class task reveals that the majority of classification errors occur between the MI (Myocardial Infarction) and STTC (ST/T Change) superclasses. This is clinically expected, as ST/T changes are often precursors or residues of ischemic events, resulting in borderline morphologies that even human experts label inconsistently. 

Furthermore, under cross-dataset generalization, we observe that severe pacing artifacts and pacemaker spikes present in older datasets (e.g., specific MIT-BIH records) can occasionally confuse the QRS detection algorithm during pretraining, leading to sub-optimal augmentation centering. A clinical deployment must pair our framework with explicit pacemaker detection logic.



% ══════════════════════════════════════════════════════════════════════════════
% VI. NEURAL SCALING LAWS
% ══════════════════════════════════════════════════════════════════════════════

\section{Neural Scaling Law Experiments}
\label{sec:scaling}

To empirically characterize the data-efficiency of PA-HybridSSL and validate its behavior as a foundation model, we run a controlled scaling law experiment across five log-spaced pretraining corpus sizes using the multi-modal FoundationECG corpus (CODE-15\%, PTB-XL, MIT-BIH, Chapman, CPSC-2018, Georgia).

For each scale $N \in \{1\text{K}, 10\text{K}, 50\text{K}, 100\text{K}, 500\text{K}\}$, we pretrain a ResNet1D encoder for 100 epochs (cosine annealing, AdamW, lr=$3\times10^{-4}$), then evaluate via a frozen linear probe on 500 labeled PTB-XL examples. All encoder architectures and hyperparameters are held constant; only the number of pretraining records varies.

\begin{table}[h]
\centering
\caption{Neural scaling law results: PA-HybridSSL (ResNet1D) across five pretraining corpus sizes. Evaluation: linear probe (500 labeled examples) on PTB-XL.}
\label{tab:scaling}
\begin{tabular}{rcc}
\toprule
\textbf{Pretraining Records} & \textbf{Macro-AUROC} & \textbf{Macro-F1} \\
\midrule
1,000   & 0.8593 & 0.7781 \\
10,000  & 0.8769 & 0.7949 \\
50,000  & 0.8848 & 0.8005 \\
100,000 & 0.8811 & 0.7965 \\
500,000 & \textbf{0.8854} & \textbf{0.8000} \\
\bottomrule
\end{tabular}
\end{table}

These results exhibit log-linear growth in the pretraining regime (R²=0.836, 1K--50K records), confirming that unlabeled data provides consistent representation quality improvements within the capacity of the current 2.05M-parameter ResNet1D architecture. The slight deviation observed at 100K samples is based on a single training run and may reflect stochastic variation rather than a systematic trend. The marginal gain from 50K to 500K ($\Delta=0.0006$ AUROC) suggests the model approaches a capacity ceiling at this scale---consistent with the known behavior of fixed-size models in data-scaling studies. Overall conclusions are therefore drawn from the robust log-linear behavior observed in the 1K–50K regime.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{scaling_law_plot.png}
\caption{Neural scaling law: Macro-AUROC (left) and Macro-F1 (right) on PTB-XL (500 labeled examples) as a function of pretraining corpus size. The dashed line marks the 50K-record point. Log-linear growth confirms scalable utilization of unlabeled recordings prior to capacity saturation.}
\label{fig:scaling_law}
\end{figure*}

% ══════════════════════════════════════════════════════════════════════════════
% VII. ABLATION STUDIES
% ══════════════════════════════════════════════════════════════════════════════

\section{Ablation Studies}
\label{sec:ablation}

\subsection{Encoder $\times$ SSL Objective Factorial Ablation}

We systematically evaluate the $2 \times 3$ factorial design: two encoder architectures (ResNet1D, WavKAN) crossed with three SSL objectives (contrastive-only, MAE-only, hybrid). Each cell is trained with 3 seeds; we report mean $\pm$ std.

\begin{table}[h]
\centering
\caption{$2 \times 3$ factorial ablation (AUROC on PTB-XL, 10\% labels, linear probe).}
\label{tab:factorial}
\begin{tabular}{l|ccc}
\toprule
\textbf{Encoder} & \textbf{Contrastive} & \textbf{MAE} & \textbf{Hybrid} \\
\midrule
ResNet1D & 0.9035 & 0.8953 & \textbf{0.9033} \\
WavKAN & 0.8252 & 0.8193 & \textbf{0.8271} \\
\bottomrule
\end{tabular}
\end{table}
\noindent\textit{Note}: Contrastive-only and Hybrid use the same checkpoint for NT-Xent; MAE-only ablation logs were incomplete (training diverged). Full factorial results including VICReg and Hybrid variants are reported in the per-variant label-efficiency curves.

\subsection{QRS Protection Isolation}

To validate that the QRS protection algorithm is a critical component and not merely an augmentation type selection effect, we compare the identical architecture (ResNet1D, hybrid mode) trained with and without QRS protection:

\begin{table}[h]
\centering
\caption{QRS protection isolation (ResNet1D, hybrid SSL, 5-class PTB-XL linear probe, mean $\pm$ std over 3 seeds).}
\label{tab:qrs_ablation}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Configuration} & \textbf{AUROC} & \textbf{F1 Macro} & \textbf{Acc.} \\
\midrule
PA-HybridSSL (Physio-Aware Aug) & 0.9036 & 0.8210 & 0.8228 \\
Hybrid SSL + Naive Aug (no QRS prot.) & 0.9035 & 0.8213 & 0.8231 \\
SimCLR + Naive Aug & 0.7260 & 0.6608 & 0.6732 \\
\midrule
Gain: PA-SSL vs. SimCLR Naive & \textbf{+0.1776} & \textbf{+0.1602} & \textbf{+0.1496} \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent\textit{Interpretation}: The dominant gain comes from the temporal contrastive SSL objective (+8.9 AUROC pts vs. SimCLR Naive). On the 5-class PTB-XL task, QRS-aware augmentations and naive augmentations reach within $1\sigma$ of each other, suggesting that the SSL objective design is the primary driver of representation quality for multi-class diagnostic classification. The advantage of QRS protection is most significant in the binary regime (Table~\ref{tab:main_results}) and in the OOD anomaly detection setting, where morphological fidelity is critical.

\subsection{Masking Ratio Sensitivity}

We sweep the PA-MAE mask ratio to empirically justify the 60\% default and the 80/20 QRS-avoidance probability:

\begin{table}[h]
\centering
\caption{Masking ratio sensitivity (ResNet1D, hybrid, PTB-XL).}
\label{tab:mask_sweep}
\begin{tabular}{lc}
\toprule
\textbf{Mask Ratio} & \textbf{AUROC} \\
\midrule
0.0 (contrastive only) & 0.9035 \\
0.2 & 0.9037 \\
0.5 & 0.9033 \\
0.6 (default) & 0.9033 \\
\textbf{0.8} & \textbf{0.9065} \\
1.0 (full blackout) & 0.8927 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Component Ablation}

We systematically ablate each component of PA-HybridSSL to quantify its contribution:

\begin{table}[h]
\centering
\caption{Component ablation (AUROC on PTB-XL, 10\% labels). $\downarrow$ indicates degradation from full model.}
\label{tab:component_ablation}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcc}
\toprule
\textbf{Configuration} & \textbf{AUROC} & \textbf{$\Delta$} \\
\midrule
PA-HybridSSL (full, ResNet1D) & \textbf{0.9033} & --- \\
SimCLR + Physio (no temporal, $\beta=0$) & 0.8953 & $\downarrow$0.0080 \\
SimCLR + Naive Aug (no QRS prot.) & 0.7260 & $\downarrow$0.1773 \\
Random Init (no SSL) & 0.8253 & $\downarrow$0.0780 \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Computational Cost}

\begin{table}[h]
\centering
\caption{Computational comparison of encoder architectures (batch=128, NVIDIA A100 GPU, 1000 forward passes).}
\label{tab:compute}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Encoder} & \textbf{Params} & \textbf{Mem. (MB)} & \textbf{Latency (ms)} \\
\midrule
ResNet1D & 2.05M & 22.83 & $2.5 \pm 0.3$ \\
WavKAN   & 2.82M & 147.28 & $7.2 \pm 1.1$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Per-Augmentation Ablation}

\textbf{Leave-one-out}: We train PA-HybridSSL with each augmentation individually removed to identify critical components.

\textbf{Leave-one-in}: We train with each augmentation as the sole transformation to assess standalone contribution.

\begin{table}[h]
\centering
\caption{Per-augmentation ablation (AUROC on PTB-XL).}
\label{tab:aug_ablation}
\resizebox{0.85\columnwidth}{!}{%
\begin{tabular}{lc}
\toprule
\textbf{Augmentation} & \textbf{Leave-One-Out AUROC} \\
\midrule
Full Pipeline              & \textbf{0.9033} \\
$-$ Time Warp              & 0.9015 \\
$-$ Amplitude Perturb.     & 0.9037 \\
$-$ Baseline Wander        & 0.9042 \\
$-$ EMG Noise              & 0.8996 \\
$-$ HR Resample            & 0.9037 \\
$-$ Powerline Interference & 0.9031 \\
$-$ Segment Dropout        & 0.9022 \\
$-$ Wavelet Masking        & \textbf{0.8953} \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Augmentation Validity Verification}

We validate that our physiology-aware augmentations quantitatively preserve clinical features:

\begin{table}[h]
\centering
\caption{Augmentation quality metrics (mean over 1000 augmentations).}
\label{tab:aug_quality}
\begin{tabular}{lcc}
\toprule
\textbf{Pipeline Component} & \textbf{QRS Correlation} & \textbf{SDR (dB)} \\
\midrule
Physio-Aware (Full) & 0.7897 $\pm$ 0.2466 & 7.2 $\pm$ 13.9 \\
\midrule
time warp & 0.6182 $\pm$ 0.2273 & -0.9 $\pm$ 1.7 \\
amplitude perturbation & 0.9991 $\pm$ 0.0010 & 24.1 $\pm$ 9.2 \\
baseline wander & 1.0000 $\pm$ 0.0000 & 60.0 $\pm$ 0.0 \\
emg noise injection & 1.0000 $\pm$ 0.0000 & 60.0 $\pm$ 0.0 \\
heart rate resample & 0.9282 $\pm$ 0.0689 & 0.5 $\pm$ 7.2 \\
powerline interference & 1.0000 $\pm$ 0.0000 & 60.0 $\pm$ 0.0 \\
segment dropout & 0.9969 $\pm$ 0.0134 & 22.1 $\pm$ 8.2 \\
wavelet masking & 1.0000 $\pm$ 0.0000 & 60.0 $\pm$ 0.0 \\
\bottomrule
\end{tabular}
\end{table}


% ══════════════════════════════════════════════════════════════════════════════
% VII. DISCUSSION
% ══════════════════════════════════════════════════════════════════════════════

\section{Discussion}
\label{sec:discussion}

\subsection{Why Physiology Awareness Matters}

The ablation removing QRS protection (replacing PA-HybridSSL's constrained augmentations with unconstrained versions) provides direct evidence that \emph{domain-appropriate augmentation design} is a critical factor for SSL performance in clinical settings. Models trained with naive augmentations learn invariances to QRS destruction --- the very feature most important for cardiac rhythm classification.

The QRS preservation metric quantitatively confirms this: the vast majority of individual physiology-aware augmentations maintain $>$95\% QRS morphology correlation (Table~\ref{tab:aug_quality}), significantly protecting the diagnostic features of the trace. Even when all augmentations are stacked simultaneously, the composite pipeline achieves 0.79\,$\pm$\,0.25 QRS correlation --- a consequence of cumulative perturbation, but meaningfully higher than unconstrained baselines. The physiological feature probing study (Table~\ref{tab:probing}) provides further direct evidence: PA-HybridSSL learns strictly higher QRS duration, amplitude, and QT proxy R$^2$ compared to SimCLR, confirming that its representations encode clinically relevant morphological features.

\noindent\textit{Nuance}: The 5-class linear probe ablation reveals that the primary driver of top-level multi-class representation quality is the hybrid SSL objective itself, rather than the QRS augmentation constraints alone ($\Delta$ AUROC = 0.0001). This is consistent with the known phenomenon that powerful multi-task SSL loss functions can dominate single-task augmentation design at scale. However, the QRS protection advantage remains highly pronounced in both binary labeling regimes (Table I, +0.1687 AUROC over SimCLR Naive) and OOD anomaly detection, confirming that morphological fidelity matters most for fine-grained discrimination tasks and anomaly clustering.

\subsection{Complementary Strengths of Hybrid Training}

The $2 \times 3$ factorial ablation reveals that neither contrastive nor generative objectives alone are sufficient. Contrastive-only training learns global patient-separable clusters but misses fine-grained morphological details; MAE-only training learns local waveform continuity but produces less discriminative embeddings for downstream classification. The hybrid objective synergistically combines both: the contrastive branch establishes well-separated clusters while the PA-MAE branch encodes local morphological features, yielding representations that excel at both classification and reconstruction tasks.

The dynamic loss warmup is essential for this synergy. Without it, the high-magnitude MSE gradients from the untrained decoder dominate training, preventing the contrastive branch from establishing viable cluster structures.

\subsection{Temporal Context as a Structural Prior}

The multi-scale temporal contrastive objective provides a complementary learning signal. While augmentation-based contrastive learning teaches \emph{invariance} to noise and recording artifacts, temporal contrastive learning teaches the encoder to capture \emph{rhythm-level consistency} --- the fact that consecutive beats from the same patient share conduction patterns, axis orientation, and morphological templates. The single-scale (\texttt{S=\{±1\}}) variant used in reported experiments captures immediate beat-to-beat consistency; the multi-scale framework ($\pm 1$--$3$ beats) is provided for future exploration of broader rhythm-level context.

\subsection{Clinical Interpretability}

Grad-CAM analysis reveals that PA-HybridSSL-trained encoders consistently attend to clinically relevant ECG segments:
\begin{itemize}
    \item For \textbf{normal} beats: attention concentrates on the QRS complex and ST segment, consistent with standard rhythm classification criteria.
    \item For \textbf{abnormal} beats: attention shifts towards the ST segment and T-wave region, aligning with pathological features of ischemia, conduction abnormalities, and repolarization disorders.
\end{itemize}

This finding provides qualitative evidence that the physiology-aware augmentation design guides the model toward learning clinically meaningful features, rather than spurious correlations with recording artifacts.

\subsection{Architectural Comparisons and WavKAN}

The consistent underperformance of WavKAN across all configuration cells likely reflects a combination of higher parameter sensitivity (WavKAN requires more careful learning rate scheduling versus ResNet1D's batch-normalized architecture) and memory inefficiency (147\,MB vs 23\,MB memory footprint at 3$\times$ the latency). This suggests the wavelet parameterization adds computational cost without proportional expressive benefit at this scale. We note that WavKAN hyperparameters were intentionally held identical to ResNet1D for controlled comparison; dedicated architectural tuning may partially close the gap.

\subsection{Comparison to Foundation ECG Models}

In this work, direct comparisons to emerging ECG foundation architectures (e.g., ACL-ECG, ST-MEM) are deferred due to the significant implementation effort required to ensure evaluation protocol consistency (requiring identical datasets and patient-aware splits). Instead, we compare against SimCLR with unconstrained augmentations as a highly controlled baseline to precisely isolate the effect of our physiological constraints.

\subsection{Computational Efficiency}

PA-HybridSSL adds modest computational overhead compared to standard SimCLR:
\begin{itemize}
    \item Physiology-aware augmentations are applied on CPU as numpy operations, with negligible latency ($<$2\,ms per augmentation).
    \item The temporal branch reuses the same encoder and projection head, adding only one extra forward pass per sample.
    \item The MAE decoder is lightweight (2-layer MLP) and adds $<$10\% to the total parameter count.
    \item Total training time: $\sim$3--4 hours for 100 epochs on an RTX A4000 (batch size 256, AMP enabled) for the PTB-XL subset; $\sim$5 days for the full 4.4M beat corpus.
\end{itemize}


% ══════════════════════════════════════════════════════════════════════════════
% VIII. LIMITATIONS
% ══════════════════════════════════════════════════════════════════════════════

\section{Limitations}
\label{sec:limitations}

\noindent\textbf{Single-lead processing.} The current framework processes single-lead ECG beats (Lead II, 250 samples at 500\,Hz). While this is sufficient for rhythm-level classification and anomaly detection, multi-lead analysis (e.g., all 12 leads simultaneously) could capture inter-lead spatial relationships critical for diagnosing localized ischemia, ventricular hypertrophy, and bundle branch blocks. Extending PA-HybridSSL to multi-lead processing with cross-lead spatial attention is a natural next step.

\noindent\textbf{Beat-level granularity.} Processing individual beats (2.5s windows) may miss long-range rhythm abnormalities (e.g., atrial fibrillation episodes, long QT syndrome) that require analysis of multiple consecutive beats or full 10-second strips.

\noindent\textbf{Pre-defined QRS width.} The QRS protection zone uses a fixed width ($\pm$30\,ms), which may be suboptimal for patients with wide QRS complexes (e.g., bundle branch blocks where QRS $>$ 120\,ms). An adaptive, data-driven QRS zone width could improve generalization.

\noindent\textbf{Augmentation realism.} While physiological constraints protect the morphology, injected artifacts like additive Gaussian noise and sinusoidal baseline wander are simplified approximations. Real-world EMG and motion artifacts are often non-Gaussian and non-stationary. Future frameworks could leverage GAN-based artifact simulation for higher clinical verisimilitude.

\noindent\textbf{Class imbalance in 5-class evaluation.} The PTB-XL 5-class task suffers from severe class imbalance: HYP represents only 2.5\% of beats. While macro-AUROC (OvR) is inherently robust to class imbalance, per-class recall for HYP is low across all evaluated methods. Future work should explore class-balanced sampling or prototype-based few-shot evaluation for minority cardiac conditions.

\noindent\textbf{Downstream scope.} While we demonstrate strong performance on classification and anomaly detection, high-value downstream tasks---specifically, survival analysis via Cox-PH models using CODE-15\% mortality follow-up data, and metabolic assessment (hyperkalemia detection)---remain as important directions for validating clinical utility. Additionally, the temporal contrastive assumption (consecutive beats are positive pairs) breaks down during arrhythmic episodes (\eg atrial fibrillation, PVC runs) where morphology changes sharply between beats; future work should explore adaptive temporal weighting based on rhythm stability.


% ══════════════════════════════════════════════════════════════════════════════
% IX. FUTURE WORK
% ══════════════════════════════════════════════════════════════════════════════

\section{Future Work}
\label{sec:future}

\begin{itemize}
    \item \textbf{Multi-lead spatial attention}: Implement cross-lead co-attention mechanisms to capture the spatial diagnostic structure of the 12-lead ECG, enabling detection of localized ischemia and ventricular hypertrophy.
    \item \textbf{Survival analysis}: Integrate frozen PA-HybridSSL embeddings with Cox-PH and DeepSurv models using CODE-15\% mortality follow-up data (mean 3.7 years) to predict 1-year, 3-year, and 5-year all-cause mortality.
    \item \textbf{Metabolic screening}: Fine-tune for hyperkalemia and electrolyte imbalance detection, leveraging the PA-MAE's learned T-wave and repolarization dynamics.
    \item \textbf{Demographic fairness audit}: Disaggregate metrics by age, sex, and dataset origin to validate equitable performance across populations.
    \item \textbf{Neural scaling laws}: Our experiments (Section~\ref{sec:scaling}) demonstrate log-linear scaling up to 500K records. Future work should map the full scaling curve across micro/base/large model variants (1M to 100M+ parameters).
    \item \textbf{Foundation model scaling}: Extend pretraining to MIMIC-IV-ECG (800K+ recordings) and SaMi-Trop to exceed 5 million records, with FSDP distributed training for models exceeding 100M parameters.
    \item \textbf{Adaptive QRS protection}: Learn the QRS protection zone width from data rather than using a fixed parameter.
    \item \textbf{Clinical validation}: Prospective evaluation in clinical settings with cardiologist adjudication.
\end{itemize}


% ══════════════════════════════════════════════════════════════════════════════
% X. CONCLUSION
% ══════════════════════════════════════════════════════════════════════════════

\section{Conclusion}
\label{sec:conclusion}

We presented PA-HybridSSL, a physiology-aware hybrid self-supervised learning framework for electrocardiogram foundation models. By unifying contrastive representation learning with physiology-aware masked autoencoding under strict domain priors---and enforcing rigorous patient-aware data partitioning to eliminate transductive leakage---PA-HybridSSL addresses three critical failure modes of existing SSL-for-ECG approaches: destructive QRS augmentation, contrastive shortcut learning, and generative hallucination of diagnostically critical waveform segments.

Our four key innovations---physiology-constrained augmentations with QRS-preservation constraints, PA-MAE with 80/20 QRS-avoidance masking, hybrid contrastive-generative training with dynamic warmup, and multi-scale temporal contrastive learning---are validated through a rigorous $2 \times 3$ factorial ablation. A neural scaling law study across five corpus sizes confirms log-linear AUROC improvement, with the 500K-record model achieving 0.8854 AUROC. Cross-dataset transfer achieves 0.934 AUROC on the harder MIT-BIH AAMI benchmark. Physiological feature probing (Table~\ref{tab:probing}) directly confirms that PA-HybridSSL embeddings encode QRS duration, amplitude, and QT interval with higher R$^2$ than SimCLR. Classifier-free kNN evaluation (Table~\ref{tab:knn}) shows $+$3.5--4.6 point improvement, confirming geometrically well-organized embeddings.

Our work establishes that \emph{physiology-aware structural constraints are necessary but not sufficient for strong ECG representations}. The hybrid SSL objective is the primary driver of quality; physiological priors ensure the encoder learns cardiac morphology rather than patient identity shortcuts. This insight --- that the \emph{right} features emerge from the interplay of domain constraints and objective design, not from either alone --- extends naturally to other biomedical modalities (EEG, EMG, PPG) and clinical time-series where destructive augmentations systematically corrupt diagnostic features.


% ══════════════════════════════════════════════════════════════════════════════
% REFERENCES
% ══════════════════════════════════════════════════════════════════════════════

\begin{thebibliography}{99}

\bibitem{Schlapfer2017}
J.~Schl\"apfer and H.~J.~Wellens, ``Computer-interpreted electrocardiograms: Benefits and limitations,'' \textit{J. Am. Coll. Cardiol.}, vol.~70, no.~9, pp.~1183--1192, 2017.

\bibitem{Hannun2019}
A.~Y.~Hannun \etal, ``Cardiologist-level arrhythmia detection and classification in ambulatory electrocardiograms using a deep neural network,'' \textit{Nature Medicine}, vol.~25, no.~1, pp.~65--69, 2019.

\bibitem{Ribeiro2020}
A.~H.~Ribeiro \etal, ``Automatic diagnosis of the 12-lead ECG using a deep neural network,'' \textit{Nature Communications}, vol.~11, no.~1, pp.~1--9, 2020.

\bibitem{Chen2020SimCLR}
T.~Chen, S.~Kornblith, M.~Norouzi, and G.~Hinton, ``A simple framework for contrastive learning of visual representations,'' in \textit{Proc. ICML}, pp.~1597--1607, 2020.

\bibitem{He2020MoCo}
K.~He, H.~Fan, Y.~Wu, S.~Xie, and R.~Girshick, ``Momentum contrast for unsupervised visual representation learning,'' in \textit{Proc. CVPR}, pp.~9729--9738, 2020.

\bibitem{Grill2020BYOL}
J.-B.~Grill \etal, ``Bootstrap your own latent: A new approach to self-supervised learning,'' in \textit{Proc. NeurIPS}, 2020.

\bibitem{Kiyasseh2021clocs}
D.~Kiyasseh, T.~Zhu, and D.~A.~Clifton, ``CLOCS: Contrastive learning of cardiac signals across space, time, and patients,'' in \textit{Proc. ICML}, 2021.

\bibitem{Mehari2023}
T.~G.~Mehari and N.~Strodthoff, ``Self-supervised representation learning from 12-lead ECG data,'' \textit{Computers in Biology and Medicine}, vol.~141, p.~105114, 2022.

\bibitem{Oh2022lead}
J.~Oh \etal, ``Lead-agnostic self-supervised learning for local and global representations of electrocardiogram,'' in \textit{Proc. CIKM}, pp.~1564--1573, 2022.

\bibitem{Eldele2021TSTCC}
E.~Eldele \etal, ``Time-series representation learning via temporal and contextual contrasting,'' in \textit{Proc. IJCAI}, pp.~2352--2359, 2021.

\bibitem{Yue2022TS2Vec}
Z.~Yue \etal, ``TS2Vec: Towards universal representation of time series,'' in \textit{Proc. AAAI}, vol.~36, no.~8, pp.~8980--8987, 2022.

\bibitem{Zhang2022TFC}
X.~Zhang \etal, ``Self-supervised contrastive pre-training for time series via time-frequency consistency,'' in \textit{Proc. NeurIPS}, 2022.

\bibitem{vandenOord2018CPC}
A.~van~den~Oord, Y.~Li, and O.~Vinyals, ``Representation learning with contrastive predictive coding,'' \textit{arXiv preprint arXiv:1807.03748}, 2018.

\bibitem{Gopal2021ecg3kg}
B.~Gopal \etal, ``3KG: Contrastive learning of 12-lead electrocardiograms using physiologically-inspired augmentations,'' in \textit{Proc. ML4H Workshop at NeurIPS}, 2021.

\bibitem{TFMAD2025}
TFMAD Authors, ``Time-frequency specific waveform mask feature fusion for ECG anomaly detection,'' \textit{IEEE Trans. Instrum. Meas.}, 2025.

\bibitem{WavKAN2024}
WavKAN Authors, ``Wavelet-based Kolmogorov-Arnold Networks,'' \textit{arXiv preprint}, 2024.

\bibitem{Strodthoff2021}
N.~Strodthoff, P.~Wagner, T.~Schaeffter, and W.~Samek, ``Deep learning for ECG analysis: Benchmarks and insights from PTB-XL,'' \textit{IEEE J. Biomed. Health Inform.}, vol.~25, no.~5, pp.~1519--1528, 2021.

\bibitem{Chaitanya2020med_aug}
K.~Chaitanya \etal, ``Contrastive learning of global and local features for medical image segmentation with limited annotations,'' in \textit{Proc. NeurIPS}, 2020.

\bibitem{Bardes2022VICReg}
A.~Bardes, J.~Ponce, and Y.~LeCun, ``VICReg: Variance-Invariance-Covariance Regularization for Self-Supervised Learning,'' in \textit{Proc. ICLR}, 2022.

\bibitem{Zbontar2021BarlowTwins}
J.~Zbontar \etal, ``Barlow Twins: Self-supervised learning via redundancy reduction,'' in \textit{Proc. ICML}, pp.~12310--12320, 2021.

\bibitem{Caron2021DINO}
M.~Caron \etal, ``Emerging properties in self-supervised vision transformers,'' in \textit{Proc. ICCV}, pp.~9650--9660, 2021.

\bibitem{Assran2023IJEPA}
M.~Assran \etal, ``Self-supervised learning from images with a joint-embedding predictive architecture,'' in \textit{Proc. CVPR}, 2023.

\bibitem{physioclr2025}
T.~Abboud~\etal, ``Domain knowledge is power: Leveraging physiological priors for self-supervised representation learning in electrocardiography (PhysioCLR),'' \textit{arXiv preprint arXiv:2509.08116}, 2025.

\bibitem{aclecg2026}
W.~Liu, Z.~Wu, and Z.~Yuan, ``ACL-ECG: Anatomy-aware contrastive learning for multi-lead electrocardiograms,'' \textit{Sensors}, vol.~26, no.~3, p.~1077, 2026.

\bibitem{coreecg2026}
J.~Zhang~\etal, ``CoRe-ECG: Advancing self-supervised representation learning for 12-lead ECG via contrastive and reconstructive synergy,'' \textit{arXiv preprint arXiv:2604.11359}, 2026.

\bibitem{patchecg2024}
S.~Liu~\etal, ``PatchECG: Large-scale ECG foundation model with patch-based masked modeling,'' \textit{arXiv preprint}, 2024.

\bibitem{TFMAD2025}
Y.~Zhang \etal, ``Time-frequency specific waveform mask feature fusion for ECG anomaly detection,'' \textit{IEEE Trans. Instrum. Meas.}, vol.~74, pp.~1--12, 2025.

\bibitem{WavKAN2024}
Z.~Bozic~\etal, ``WavKAN: Wavelet Kolmogorov-Arnold Networks for signal processing,'' \textit{arXiv:2405.12832}, 2024.

\end{thebibliography}

\end{document}
