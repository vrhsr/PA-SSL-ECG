# Core ML / Deep Learning
torch>=2.1.0
torchvision>=0.16.0

# Scientific computing
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
scikit-learn>=1.4.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
umap-learn>=0.5.5

# ECG / Biomedical data loading
wfdb>=4.1.2
neurokit2>=0.2.7
h5py>=3.10.0          # Required for CODE-15% HDF5 dataset

# Training utilities
tqdm>=4.66.0
pyyaml>=6.0
tensorboard>=2.15.0

# Statistical analysis
statsmodels>=0.14.0

# ──────────────────────────────────────────────────────────────────
# OPTIONAL: GPU/Linux-only extras — install manually on GPU server
# ──────────────────────────────────────────────────────────────────
# mamba-ssm>=1.2.0          # Mamba SSM encoder (CUDA required, Linux only)
#   pip install mamba-ssm    # Run on GPU server, NOT on Windows laptop
#
# transformers>=4.38.0       # BioClinicalBERT for ECG-CLIP (Phase 3)
#   pip install transformers
